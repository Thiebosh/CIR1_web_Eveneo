fonction utile : print_r($array);
pb : ne peut pas afficher un string contenant un espace avec <?= ?> dans la value d'un form

travail restant :
    gestion des erreurs
    refonte graphique
        formulaire : centrés. en tableau?
        erreurs et alerte : liste (erreur, alerte, confirmation) dans une case survolable sous le menu (?), redirection vers la page d'origine?
    tout mettre en simple guillemets (echo / php)
    implémenter dernières idées
        désinscription
        afficher nb de personnes inscrites (orga)
        afficher bouton de suppression au survol (orga - mois)
        afficher jours fériés d'une couleur différente
